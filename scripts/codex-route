#!/usr/bin/env python3
"""Compatibility entry point for the integrated native routing controller."""
from pathlib import Path
import sys
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "routing"))
from codex_routing.cli import main
raise SystemExit(main())
